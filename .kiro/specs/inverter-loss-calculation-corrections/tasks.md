# Implementation Plan

- [x] 1. Freeze the unfixed baseline and write the bug-condition exploration test
  - **Property 1: Bug Condition** - Conservative Corrected Pipeline
  - **CRITICAL**: Complete this standalone task before any implementation. Run the corrected-behavior property against the unfixed baseline and expect failure; do not weaken assertions, alter the baseline, or fix code during characterization.
  - Freeze the named Git commit ID/tree hash, complete system-schema artifact, and a closed firmware source/config manifest. Record immutable before hashes for every firmware source/config artifact used by the build or timing oracle; firmware additions, removals, renames, configuration changes, and content changes are forbidden.
  - Freeze one immutable `SpecBaselineOwner`-approved legacy manifest before implementation. Require exact namespaced key-set equality for `TOP_LEVEL:3.1` through `TOP_LEVEL:3.12`; closed `EVIDENCE:` keys `record_id`, `subject`, `related_subject`, `field`, `value`, `unit`, `EvidenceClass`, `ConflictStatus`, `manufacturer`, `rating_class`, `artifact_id`, `artifact_version`, `artifact_hash`, `locator`, `user_conditions`, `source_conditions`, `applicability`, `relationship_rationale`, `formula_or_algorithm`, `dependency_record_ids`, `instrument_ids`, `calibration_versions`, `measurement_boundary`, `sample_coverage`, `method`, `uncertainty_or_bound`, `adjudicator`, `transition_timestamp`, `transition_reason`, `trace_links`, `synthetic_marker`; and closed `OUTPUT:` keys `loss_categories`, `efficiency`, `temperatures`, `thermal_status`, `standby`, `firmware_implementation_status`, `hardware_measurement_status`, `device_comparison`, `qualification`, `residual_sources`, `boundary`, `availability`, `freshness`, `applicability`, `result_basis`, `canonical_value`, `uncertainty_or_bound`, `artifact_id`, `artifact_version`, `artifact_hash`, `trace_identity`, `dependency_set`, `synthetic_marker`, `headline`.
  - Map every namespaced key exactly once to an immutable source line/range or one adapter of type `STATIC_TEXT_PARSER`, `LEGACY_FUNCTION_CAPTURE`, or `LEGACY_SUBPROCESS_CAPTURE`. Freeze adapter-code, input, environment/container, command-or-entry-point, and captured-output hashes; freeze executable/dependency hashes for subprocess adapters.
  - Freeze every mapping as exactly `BASELINE_BEHAVIOR(canonical_observation, frozen_acceptance_rule)` or `NO_BASELINE_BEHAVIOR` with no observation. Freeze applicability, eligibility, local bug/fix predicates, adapter choice, and any deterministic sampling algorithm, seed, ordered population, and size.
  - Encode the design’s `isBugCondition(input)` and assert `expectedBehavior(result)`. Cover evidence/class/conflict misuse; pending consumption; `MaterialResult` lattice/freshness errors; noncanonical decisions; digital-oracle differences; analog/settling defaults; residual erasure/overlap; legacy coverage gaps; excluded dependencies; thermal/uniqueness errors; comparison order/double count; synthetic-real leakage; coupled control axes; firmware drift; and unsafe publication.
  - Include deterministic fixtures for IRL40SC209 0.60 mΩ misclassification, IRL40T209 0.59/0.72 mΩ promotion, unresolved 1.3/1.7 mΩ rows, and p=10/1/0 timing. The timing fixtures must expose stale `N=2286`/`H=1143`, even-period/exact-50%, `φ=pT/200`, wrong actual-carrier, and old-vector assumptions; p=10 adjusted count `15`; tick-2284 `SR_B_ON`-before-`SR_B_OFF`; p=1/p=0 adjusted LV counts other than `8`; coercion of unavailable suppressed-pulse/complete-adjusted-SR/complete-aggregate data to empty, zero, no transitions, no switching, no gate loss, or loss-free behavior; and erasure or misownership of the p=0 one-tick asymmetry. Also cover caller-selected settling, loaded 36 W/42.8 W no-load promotion, incomplete 48 V qualification, stale descendants, and rounded acceptance.
  - Include deterministic comparison-gate counterexamples for absent, late, nonfinite, unitless, out-of-domain, or changed `T_ref`; any state other than exactly two distinct immutable non-aliased/non-reused pre-realistic gate-outcome artifacts (one each for `CONTROLLED_THREEFOLD` and `ENDPOINT_SENSITIVITY`) with no extras; a missing, duplicate, mutable, malformed, invalid, wrong-gate, hash-mismatched, aliased, reused, or late-created artifact; zero, duplicate, or multiple outcomes; and an invalid completed outcome. Cover the exact serialized `canonical_code` field and reject generic, alias, unrecognized, wrong-gate, and predicate-mismatched codes. Cover controlled ineligibility only for the three requirement-2.30 iff rows after every other 2.29 precondition passes, and endpoint ineligibility only as `INELIGIBLE_NO_POSITIVE_CURRENT_CASE` after all endpoint/evidence/policy/fixed-input/closed-candidate-set prerequisites pass and no eligible case exists. Reject missing, extra, duplicate, or mismatched all-and-only predicate sets; missing or nonspecific reasons; absent, noncanonical, or nonzero evaluated count; absent, duplicate, malformed, or true `completion_claim`, `pass_claim`, or `success_claim`; missing evidence/policy, invalid endpoints, unfixed inputs, open/mutable candidate sets, invalid completed outcomes, and every bypass. Assert every malformed, rejected, or unresolved applicable gate blocks progression and makes every workflow-authorization-dependent `MaterialResult` `UNAVAILABLE`, while unrelated results retain their independently derived availability and freshness. Add positive fixtures proving that each permitted valid ineligibility is a closed-union authorization for progression without gate completion, pass, or success; after authorization, independently classify realistic outputs as `AVAILABLE`, `PROVISIONAL`, and `UNAVAILABLE` from each output’s own admissible inputs/evidence, dependencies, result-specific acceptance conditions, and applicable workflow authorizations, and prove no downstream status upgrades, relabels, or mutates the ineligibility artifact or its false claims.
  - Record each minimal counterexample with fixture ID, bug-condition branch, canonical input, actual/expected behavior, affected artifact IDs/hashes, and exact failure. Identify every key requiring `NO_BASELINE_BEHAVIOR` because no real legacy observation exists.
  - Missing manufacturer, hardware, analog MODREF, product-threshold/settling, thermal, or root-certificate evidence must remain local and non-gating as `PROVISIONAL`, `UNAVAILABLE`, or `UNPROVEN`; never invent evidence, policy, values, or firmware behavior to force a result.
  - **EXPECTED OUTCOME BEFORE FIX**: The property fails reproducibly and its counterexamples remain frozen for post-fix rerun.
  - _Requirements: 1.1–1.12, 2.1–2.42, 3.11, 3.12_

- [x] 2. Execute the frozen observation-first preservation suite on the unfixed baseline
  - **Property 2: Preservation** - Frozen Legacy Routing
  - **IMPORTANT**: This is a standalone pre-implementation task. Use only the commit/tree, manifest, routes, adapters, hashes, predicates, observations, acceptance rules, and optional sampling frozen in task 1.
  - Validate exact closed key cardinality/namespaces, extraction routes, adapter types/hashes, commit/tree binding, predicates, and sampling. Reject omitted, duplicate, unknown, unnamespaced, mutable, drifted, or post-freeze mappings.
  - For each `BASELINE_BEHAVIOR`, generate the frozen population, select eligible non-bug cases, apply only pre-frozen sampling if declared, and compare every evaluated case with its immutable canonical observation.
  - For each `NO_BASELINE_BEHAVIOR`, prohibit any fabricated legacy observation, use the frozen eligible fix-check predicate, and execute at least one corrected-behavior case.
  - Emit ordered case IDs and `generated`, `eligible`, `sampled`, `evaluated`, and `excluded` counts per key; require `generated ≥ eligible ≥ sampled = evaluated ≥ 1`. Any skipped eligible baseline case without frozen sampling, zero evaluation, missing nested key, or route/hash drift fails coverage.
  - Preserve actual non-bug behavior for all applicable inverter loss categories; fixed physical transformer-ratio current; eligible `Irms² × 2 × RDS(on,Tj) / m`; downstream-only system composition; explicit uncertainty; active/transition physics; independent qualification; exact scenario boundaries; calculated/modelled labels; complete `HOT_START_TRANSIENT`; and the 0.60 mΩ `USER_SPECIFIED_TARGET` classification. Preserve one finite unit-bearing in-domain `T_ref` frozen before either gate; exactly two distinct immutable non-aliased/non-reused pre-realistic gate-outcome artifacts, one each for `CONTROLLED_THREEFOLD` and `ENDPOINT_SENSITIVITY`, with no extras; exactly one gate-specific closed-union outcome per artifact; exact `canonical_code`; controlled three-code iff routing only when every other 2.29 precondition passes; sole endpoint `INELIGIBLE_NO_POSITIVE_CURRENT_CASE` routing only when all endpoint/evidence/policy/fixed-input/closed-candidate-set prerequisites pass and no eligible case exists; all-and-only unsatisfied predicates, a specific reason, canonical evaluated count zero, and exactly-once `completion_claim=false`, `pass_claim=false`, and `success_claim=false`. Preserve each valid ineligibility as a progression-authorizing closed-union outcome only, never completion, pass, or success. Preserve independent downstream realistic-output classification as `AVAILABLE`, `PROVISIONAL`, or `UNAVAILABLE` from that output’s local admissible inputs/evidence, dependencies, result-specific acceptance conditions, and applicable workflow authorizations; no downstream result may upgrade, relabel, or mutate the ineligibility artifact. Preserve rejection of wrong/generic/alias/predicate-mismatched codes, malformed/invalid/late/duplicate/reused artifacts, invalid completed outcomes, missing evidence/policy, invalid endpoints, and every bypass: each malformed, rejected, or unresolved applicable gate blocks progression and makes every workflow-authorization-dependent `MaterialResult` `UNAVAILABLE`, while unrelated results remain unchanged.
  - Missing manufacturer, hardware, analog, settling, thermal, or root evidence must preserve the applicable local non-gating `PROVISIONAL`, `UNAVAILABLE`, or `UNPROVEN` behavior and must not become a fabricated baseline or overall failure.
  - **EXPECTED OUTCOME BEFORE FIX**: Every baseline comparison and no-baseline fix route passes with non-vacuous machine-readable coverage.
  - _Requirements: 2.13, 2.14, 2.41, 2.42, 3.1–3.12_

- [x] 3. Implement the evidence-conservative inverter-loss correction
  - Apply subtasks 3.1–3.13 in order. Work is limited to calculation, evidence, oracle, validation, trace, test, and publication artifacts; do not modify firmware source/configuration or implement firmware no-load/control behavior.
  - Missing manufacturer, hardware, analog MODREF, settling policy/observations, thermal evidence, qualification evidence, optional root certificate, or independent ESP-IDF peripheral-semantics evidence for the one-tick SR pulse must affect only the dependent decision as non-gating `PROVISIONAL`, `UNAVAILABLE`, or `UNPROVEN`; never supply a fallback, workaround, invented value/list/count, or stronger claim.
  - _Bug_Condition: `isBugCondition(input)` from the design_
  - _Expected_Behavior: `expectedBehavior(result)` and Correctness Properties 1 and 3–20 from the design_
  - _Preservation: frozen manifest routing and all Preservation Requirements from the design_
  - _Requirements: 2.1–2.42, 3.1–3.12_

  - [x] 3.1 Implement canonical values/events, `MaterialResult`, trace freshness, and ATS-v2
    - Implement canonical numeric type/dimension/unit/shape/order with reduced rationals or exact finite float bits, signed-zero normalization, and NaN/infinity rejection. Implement canonical event tuples and same-tick order `OFF`, `DEADTIME_EXPIRY`, `ON`, then stable device ID.
    - Wrap every material value/status in `MaterialResult` with exact availability/freshness enums, dependency IDs/hashes, computation artifact ID/version/hash, canonical value or explicit absence, applicability, optional finite bound, and trace identity.
    - Derive `AVAILABLE` locally per result: require all of that `MaterialResult`’s admissible inputs/evidence and result-specific acceptance conditions plus a valid outcome from every applicable gate’s required closed union that authorizes the requested operation. Do not define availability as every gate passing: a valid completed outcome or permitted ineligibility may authorize its specified operation, and a correctly evaluated failed domain comparison may itself be an `AVAILABLE` result when its result-specific contract is satisfied.
    - Validate workflow authorization before local availability classification. A permitted 2.25/2.38 ineligibility may authorize only progression to realistic comparison while retaining exactly `completion_claim=false`, `pass_claim=false`, and `success_claim=false`; independently derive each downstream realistic result as `AVAILABLE`, `PROVISIONAL`, or `UNAVAILABLE` from its own inputs/evidence, dependencies, local acceptance conditions, and authorizations, without upgrading, relabeling, or mutating the gate artifact.
    - Make every malformed, rejected, unresolved, or operation-non-authorizing applicable gate return `UNAVAILABLE` for each `MaterialResult` dependent on that workflow authorization; never downgrade such a result merely to `PROVISIONAL`, and leave unrelated results unchanged. Enforce unavailable/provisional dependency propagation, absent-without-bound handling, pre-commit transitive staleness, stale-consumption blocking, fresh recomputation with a new identity, and prohibition on display/rounded input reuse.
    - Consume numerical policy only from an immutable, applicable, unexpired, hash-valid, `InverterOwner`-approved `ATS-v2`; implement exactly requirement 2.35 and the closed `relevant power`/`relevant thermal loss` definitions. No local or default tolerance is allowed.
    - Missing/invalid ATS makes only the affected decision `UNAVAILABLE` and non-gating. Missing manufacturer, hardware, analog, settling, or root evidence follows the same local status discipline.
    - Add unit/property tests for the availability/freshness product, absent bounds, dependency hash changes, display collisions, canonical events, aggregate definitions, and ATS absence/applicability. Add an authorization matrix that proves: valid completed and permitted-ineligibility outcomes can authorize progression; permitted ineligibility never becomes completion/pass/success; realistic outputs independently span `AVAILABLE`, `PROVISIONAL`, and `UNAVAILABLE` without changing that artifact; malformed/rejected/unresolved gates force all authorization-dependent results to `UNAVAILABLE`; and unrelated results remain bit-for-bit/status/trace unchanged.
    - **Property 5: MaterialResult Availability and Freshness** - Dependency-Safe Status
    - **Property 6: Canonical Numeric and Event Equality** - Exact Full-Precision Decisions
    - _Bug_Condition: MaterialResult/canonical/policy/freshness contract is violated_
    - _Expected_Behavior: all material decisions are canonical, traced, fresh, and safely unavailable when policy is absent_
    - _Preservation: calculation equations remain full precision and presentation rounding remains output-only_
    - _Requirements: 2.5, 2.34, 2.35, 2.41, 2.42, 3.5, 3.9_

  - [x] 3.2 Implement evidence metadata, conflict transitions, selection, and invalidation
    - Implement the exact independent `EvidenceClass` and `ConflictStatus` enums, class-specific closed metadata, required explicit absence forms, raw-pending rows, immutable histories, legal transition graph, selection cardinality, and terminal states.
    - Store IRL40SC209 0.60 mΩ only as `USER_SPECIFIED_TARGET` with exact subject and immutable assertion artifact unless direct exact-part evidence exists; never label it typical, maximum, guaranteed, direct, or measured.
    - Store IRL40T209 0.59/0.72 mΩ only as `RELATED_PART` with complete manufacturer/source/condition metadata and rationale. Store 1.3/1.7 mΩ and other conflicts as separate raw rows with all available metadata and explicit absences; pending rows cannot be selected, consumed, qualified, accepted, or published.
    - Permit only `NONE→PENDING`, `PENDING→ACCEPTED_FOR_FIELD`, `PENDING→REJECTED_FOR_FIELD`, and `ACCEPTED_FOR_FIELD→SUPERSEDED`; append adjudicator/time/reason/cited artifacts without mutation. Require exactly one eligible selection per tuple and at most one active accepted record.
    - Before selection changes, stale all descendants; update only exact fields supported by cited evidence and recompute with new trace identities. Missing exact-part/manufacturer evidence remains non-gating `PROVISIONAL` or `UNAVAILABLE` and does not block unrelated results.
    - Add metadata, absence, transition, terminal-state, pending-consumption, cardinality, immutable-history, stale-descendant, and unaffected-node tests.
    - **Property 3: Evidence and Conflict Independence** - Closed Class Metadata
    - **Property 4: Conflict Transition and Selection Integrity** - Immutable Adjudication
    - _Bug_Condition: evidence strength/conflict are collapsed, unsupported records are consumed, or descendants remain fresh_
    - _Expected_Behavior: only eligible exact-field evidence is selected and every changed dependency invalidates descendants_
    - _Preservation: the target and independent uncertain fields retain their non-manufacturer status_
    - _Requirements: 2.1–2.5, 2.34, 2.41, 3.5, 3.11_

  - [x] 3.3 Implement the exact 2.36 digital oracle and independent analog MODREF handling
    - Implement `p∈[0,100]`, separate polarity `s`, `H=floor(N/2)`, ideal displacement `q=pH/100`, ideal time displacement `φ=qΔt`, and `k=clamp(floor(q+0.5),0,H)` with exact half-tick ties rounded upward. Model the raw leading interval as `H` ticks and the complement as `N-H`; do not infer even `N`, exact `T/2`, or exact 50% duty.
    - Bind the hand-reviewed oracle ID/version/hash and `InverterOwner` approval to task-1 source/config hashes. Validate exactly: clock `160000000 Hz`; nominal carrier label `70000 Hz`; compiled `N=floor(160000000/70000)=2285` (odd); `H=1142`; `Δt=1/160000000 s`; `T=2285/160000000 s`; actual carrier `160000000/2285=32000000/457 Hz`; raw interval lengths `1142/1143`; LV dead time `10`; SR dead time `16`; SR lead/trail `8/8`; minimum window `18`; SR enabled iff `k>34`; comparator identities `(LV_A_ON_RESERVED,LV_A_OFF,LV_B_ON,LV_B_OFF,SR_A_ON_RESERVED,SR_A_OFF,SR_B_ON,SR_B_OFF)`; SR triplet `SR_A_OFF/SR_B_ON/SR_B_OFF`; reserved comparator value `1` with raw ON at TEZ tick 0.
    - Validate p=10 exactly: `q=114.2`, `k=114`; comparators `(1,1142,114,1256,1,106,1150,1248)`; intervals `LV_A[0,1142)`, `LV_B[114,1256)`, `SR_A[0,106)`, `SR_B[1150,1248)`; raw events `0:LV_A_HI_ON,SR_A_HI_ON; 106:SR_A_HI_OFF; 114:LV_B_HI_ON; 1142:LV_A_HI_OFF; 1150:SR_B_HI_ON; 1248:SR_B_HI_OFF; 1256:LV_B_HI_OFF` (count 8); complete adjusted events `0:LV_A_LO_OFF,SR_A_LO_OFF; 10:LV_A_HI_ON; 16:SR_A_HI_ON; 106:SR_A_HI_OFF; 114:LV_B_LO_OFF; 122:SR_A_LO_ON; 124:LV_B_HI_ON; 1142:LV_A_HI_OFF; 1150:SR_B_LO_OFF; 1152:LV_A_LO_ON; 1166:SR_B_HI_ON; 1248:SR_B_HI_OFF; 1256:LV_B_HI_OFF; 1264:SR_B_LO_ON; 1266:LV_B_LO_ON`, with complete adjusted individual event count `16` (two events at tick 0 plus 14 other individual events).
    - Validate p=1 exactly: `q=11.42`, `k=11`; comparators `(1,1142,11,1153,1,1,2284,2284)`; intervals `LV_A[0,1142)`, `LV_B[11,1153)`; raw SR edges `SR_A_ON@0,SR_A_OFF@1,SR_B_OFF@2284,SR_B_ON@2284`; raw events `0:LV_A_HI_ON,SR_A_HI_ON; 1:SR_A_HI_OFF; 11:LV_B_HI_ON; 1142:LV_A_HI_OFF; 1153:LV_B_HI_OFF; 2284:SR_B_HI_OFF,SR_B_HI_ON` (count 8); adjusted LV events `0:LV_A_LO_OFF; 10:LV_A_HI_ON; 11:LV_B_LO_OFF; 21:LV_B_HI_ON; 1142:LV_A_HI_OFF; 1152:LV_A_LO_ON; 1153:LV_B_HI_OFF; 1163:LV_B_LO_ON` with exact adjusted LV count `8`.
    - For p=1, retain `SR_DEADTIME_ADJUSTED_SUPPRESSED_PULSE=UNAVAILABLE(missing independent ESP-IDF peripheral-semantics artifact or measurement establishing whether the delayed high pulse is cancelled and what low-side transitions occur)`. Keep the complete adjusted SR list/count and complete aggregate adjusted list/count safety-relevant, non-gating `UNAVAILABLE` pending that evidence; reject coercion to an empty list, zero count, no transitions, no switching, no gate loss, or loss-free behavior.
    - Validate p=0 exactly: `q=0`, ideal `k=0`, and implementation-guarded `LV_B_ON=1`; comparators `(1,1142,1,1142,1,1,2284,2284)`; intervals `LV_A[0,1142)`, `LV_B[1,1142)`; raw SR edges `SR_A_ON@0,SR_A_OFF@1,SR_B_OFF@2284,SR_B_ON@2284`; raw events `0:LV_A_HI_ON,SR_A_HI_ON; 1:SR_A_HI_OFF,LV_B_HI_ON; 1142:LV_A_HI_OFF,LV_B_HI_OFF; 2284:SR_B_HI_OFF,SR_B_HI_ON` (count 8); adjusted LV events `0:LV_A_LO_OFF; 1:LV_B_LO_OFF; 10:LV_A_HI_ON; 11:LV_B_HI_ON; 1142:LV_A_HI_OFF,LV_B_HI_OFF; 1152:LV_A_LO_ON,LV_B_LO_ON` with exact adjusted LV count `8`.
    - For p=0, retain the same explicit `SR_DEADTIME_ADJUSTED_SUPPRESSED_PULSE=UNAVAILABLE(...)` reason and safety-relevant, non-gating `UNAVAILABLE` complete adjusted SR list/count and complete aggregate adjusted list/count. Reject coercion to empty/zero/no transitions/no switching/no gate loss/loss-free behavior. Treat the `LV_B_ON=1` one-tick asymmetry as actual implementation behavior owned exactly once and only by `LEG_COMMAND_TICK_ASYMMETRY`; do not erase it with the 1142/1143 odd-period model or classify it as ideal phase.
    - Compare all available constants, q/k values, comparator identities/values, raw events, p=10 complete adjusted events, and p=1/p=0 adjusted LV canonical event tuples, classes, counts, and orders exactly; validate one-field mutations fail. Require `SR_B_OFF` before `SR_B_ON` at tick `2284`; every adjusted OFF is `OFF`, every available delayed ON is `DEADTIME_EXPIRY`, and no event outside an available list’s exact scope/count is implied. For unavailable p=1/p=0 adjusted fields, assert preservation of explicit absence, reason, dependencies, safety relevance, and non-gating status rather than comparing fabricated lists.
    - Implement analog MODREF fields as exactly independently approved `EXPECTED(...)` or `UNAVAILABLE(missing_fields)`. Compare only available canonical unrounded analog fields under their approved field tolerance; never infer analog transfer/current/energy/loss from digital timing or another analog field.
    - Missing analog/manufacturer/hardware/settling/root or independent ESP-IDF peripheral-semantics evidence leaves available exact digital checks intact and only affected decisions non-gating `UNAVAILABLE`; do not invent analog vectors or adjusted SR/aggregate behavior.
    - **Property 7: Repository Digital Oracle** - Exact 10%, 1%, and 0% Vectors
    - **Property 8: Analog and Policy Independence** - No Digital-to-Analog Inference
    - _Bug_Condition: timing differs from the fixed oracle or analog acceptance uses self-derived/missing references_
    - _Expected_Behavior: digital values match exactly while each analog field is independently expected or safely unavailable_
    - _Preservation: valid nonzero phase, polarity, dead time, SR/freewheel timing, and implementation asymmetry remain represented_
    - _Requirements: 2.6–2.9, 2.35, 2.36, 2.41, 2.42, 3.1, 3.2, 3.6_

  - [x] 3.4 Implement typed residual ownership, settling, and no-load classification
    - Implement the exact residual enum, case-unique `ownership_id`, unique nonempty `OTHER_DECLARED` subtype, and `VALUE`/`NA(reason)`/`UNAVAILABLE(missing_fields)` semantics.
    - Validate an immutable `InverterOwner`-approved matrix covering every supported topology × p-state × gate-state × SR-state row and each closed residual-type column, with every cell exactly `REQUIRED` or `NOT_APPLICABLE(reason)`.
    - Require one record per required type and none per prohibited type; require one `LEG_COMMAND_TICK_ASYMMETRY` owner for applicable repository p=0 rows. Enforce initiating-source precedence before remaining `STORED_ENERGY_DECAY`, prohibit overlap/duplicate ownership, and never add uncertainty as physical loss.
    - At p=0 force only commanded ideal transfer/excitation to zero. Derive actual pulses/current/volt-seconds/flux/energy/loss from typed records; an unknown applicable residual is never zero and is `PROVISIONAL` only with an evaluable finite bound, otherwise `UNAVAILABLE`.
    - Reject caller-settled flags. Use only valid ATS-v2 and approved product thresholds/methods. Require contiguous `W≥max(10T,1 ms)` and passing Ires, each-period volt-seconds, cumulative volt-seconds, and end-energy comparisons before `SETTLED`.
    - Missing/invalid policy, observation, model, measurement, applicability, or approval leaves the case `TRANSITION`, retains residuals, and makes only the settled decision non-gating `UNAVAILABLE`; an available exceeded limit produces an available failed comparison and `TRANSITION`.
    - Emit all eight no-load categories with separate applicability, basis, availability, complete context, provenance, and uncertainty. Preserve loaded 36 W/42.8 W constants only in their original loaded context.
    - **Property 9: Zero-Command Residual Conservation** - Unknown Is Not Zero
    - **Property 10: Closed Residual Applicability and Ownership** - No Missing or Double-Counted Physics
    - **Property 11: Settling Classification** - Observation and Approved Policy Only
    - **Property 19: No-Load Completeness** - Every Category Classified
    - _Bug_Condition: ideal zero erases actual physics, sources overlap, or unapproved/missing settling evidence establishes settled behavior_
    - _Expected_Behavior: typed residuals remain conserved and absent settling evidence is safely non-gating_
    - _Preservation: nonzero-command and command-to-zero transition losses/stored energy remain represented_
    - _Requirements: 2.10–2.12, 2.32, 2.33, 2.35–2.37, 2.41, 3.5, 3.6, 3.9_

  - [x] 3.5 Implement the closed inverter-domain inventory and exact isolation
    - Freeze an immutable, versioned, `InverterOwner`-approved allowed/excluded inventory generated from the complete frozen system schema. Require every path exactly once in the disjoint union; reject unknown, duplicate, unclassified, mutable, incomplete, or identity-mismatched inventories.
    - Restrict the pure solver to closed `InverterInput`; prohibit direct, transitive, implicit, environment, cache, callback, generic-map, and default dependencies on every excluded upstream field.
    - Mutate every excluded path alone to at least two distinct schema/domain-valid values while holding every other field, inverter input, and DC-terminal point bit-for-bit fixed; require exact canonical equality of every output/status/trace identity/dependency set.
    - Implement named downstream-only composition and exact closed scenario-boundary tuple comparison; adding/removing/mutating composition at an unchanged terminal point cannot affect the inverter result.
    - Missing external manufacturer/hardware/analog/settling/root evidence remains locally non-gating; an invalid structural domain inventory blocks inverter-only acceptance without inventing paths.
    - **Property 12: Exact Inverter Boundary and Scenario Equivalence** - Every Excluded Path Is Isolated
    - _Bug_Condition: excluded fields influence inverter values or traces_
    - _Expected_Behavior: inverter outputs and identity are exactly invariant under every valid excluded-field mutation_
    - _Preservation: separately selected battery/charger/C-rate/round-trip analyses remain downstream-supported_
    - _Requirements: 2.17–2.19, 2.41, 2.42, 3.4, 3.8_

  - [x] 3.6 Implement cold, dynamic, steady, and optional root-verification thermal APIs
    - Implement `COLD_T0` with explicit finite physical temperatures for every applicable node inside all supported domains, no Cth requirement, and no derivative. Omitted, undeclared-hot, converged, or out-of-domain temperatures make affected results `UNAVAILABLE`.
    - Implement derivative/t>0 transient, including `HOT_START_TRANSIENT`, with positive finite dimensionally valid evidenced Cth for every dynamic node plus complete Rth, boundaries, cooling, power/time, physical initial vector, approved method/control/error artifacts, and in-domain trajectories.
    - Implement steady state with complete Rth/boundaries/cooling/loss/domain/control data and no Cth dependency. Independently mutate each unused Cth to two valid values and require exact steady output/trace invariance.
    - Use exactly lower-bound, midpoint, and upper-bound seeds, deduplicated only when canonically identical. Record all roots/residuals/domain/iterations; ATS-v2 may establish `MULTI_SEED_AGREEMENT` only.
    - Permit `UNIQUE` only with a complete-domain one-root certificate and producer-distinct independent verifier with all hashes. Missing/invalid thermal data gives local non-gating `UNAVAILABLE`; missing/invalid root evidence gives `UNPROVEN` while retaining valid steady results.
    - **Property 13: Thermal Mode Contracts** - Distinct Physical Inputs
    - **Property 14: Thermal Seed and Uniqueness Semantics** - Agreement Is Not Uniqueness
    - _Bug_Condition: modes share invalid defaults, Cth contaminates steady state, or seed agreement is overclaimed_
    - _Expected_Behavior: each API enforces its domain and optional root evidence never becomes mandatory_
    - _Preservation: complete evidenced `HOT_START_TRANSIENT` and temperature-dependent losses remain supported_
    - _Requirements: 2.20–2.24, 2.35, 2.41, 3.5, 3.9, 3.10_

  - [x] 3.7 Implement the closed comparison gates before realistic common-Tref comparison
    - Freeze exactly one finite unit-bearing `T_ref` inside both exact devices’ supported electrical-property and thermal domains before either mandatory gate executes. Reject absent, late, nonfinite, unitless, out-of-domain, or changed `T_ref`; no gate or realistic result may precede, substitute for, or retroactively validate this prerequisite.
    - Model exactly two gate identities: `CONTROLLED_THREEFOLD` and `ENDPOINT_SENSITIVITY`. Before realistic execution, require exactly two gate-outcome artifacts total: exactly one immutable artifact ID/version/hash for each gate, with distinct non-aliased identities, no reuse between gates, and no additional gate-outcome artifact. Both artifacts must be frozen before realistic execution and each must identify exactly one gate.
    - Require exactly one outcome per artifact from that gate’s closed union. The controlled union is exactly one requirement-2.29-compliant completed recorded result or exactly one requirement-2.30 ineligibility. The endpoint union is exactly one requirement-2.31-compliant completed recorded result or exactly one permitted endpoint ineligibility. Reject zero, duplicate, multiple, mixed, unknown, or otherwise invalid outcomes and every invalid completed result.
    - Serialize every ineligibility with exactly one field named `canonical_code`; reject missing/duplicate fields, aliases, generic strings, unrecognized values, and wrong-gate codes. Require a canonical evaluated-case count exactly zero, one specific nonempty reason, the all-and-only code-corresponding unsatisfied eligibility predicates, and exactly one each of `completion_claim=false`, `pass_claim=false`, and `success_claim=false`; reject extra, omitted, duplicate, malformed, noncanonical, nonzero, or true count/claim fields.
    - Implement the controlled completed path only with finite `Irms>0`, nonempty conduction, positive finite baseline RDS(on), fixed `T_ref`, and identical current waveform, topology, parallel count, timing, and all non-RDS(on) inputs. Set replacement RDS(on) to baseline/3 exactly, require replacement channel loss to equal baseline/3 within relative error `≤1e-9`, and require nonzero unrounded channel and total-loss deltas attributable to RDS(on).
    - Permit controlled ineligibility only when every 2.29 precondition other than positive `Irms` and nonempty conduction passes. Enforce the mutually exclusive exhaustive iff partition: `INELIGIBLE_ZERO_IRMS` iff finite `Irms=0` and conduction is nonempty, with predicate set `{FINITE_POSITIVE_IRMS}`; `INELIGIBLE_EMPTY_CONDUCTION_INTERVAL` iff finite `Irms>0` and conduction is empty, with `{NONEMPTY_CONDUCTION_INTERVAL}`; and `INELIGIBLE_ZERO_IRMS_AND_EMPTY_CONDUCTION_INTERVAL` iff finite `Irms=0` and conduction is empty, with both predicates. Require exact-zero baseline/replacement channel losses and delta and ratio `N/A`; negative/nonfinite current, invalid resistance, unfixed inputs, or any other failed 2.29 precondition leaves the gate unresolved and is never recoded as ineligibility.
    - Implement endpoint completed evaluation only with independently declared and evidenced finite low/high endpoints whose canonical dimensions, units, and applicability match and `low<high`; available valid applicable policy; fixed identity and every non-endpoint canonical full-precision input; and a predeclared closed immutable candidate set. Evaluate every candidate with finite positive current and nonempty conduction and record unrounded component/total outputs and deltas.
    - Permit endpoint ineligibility only with `canonical_code=INELIGIBLE_NO_POSITIVE_CURRENT_CASE`, after every endpoint/evidence/policy/fixed-input/identity/closed-candidate-set prerequisite passes and no candidate in the closed set has both finite positive current and nonempty conduction. Record all and only false `(candidate_id, eligibility_predicate)` pairs. Missing, malformed, unevidenced, nonfinite, dimension/unit/applicability-mismatched, or unordered endpoints; missing, invalid, unevidenced, or inapplicable policy; unfixed inputs; or an open/mutable candidate set is rejection, not ineligibility.
    - On every missing, malformed, invalid, mutable, aliased, reused, duplicate, extra, wrong-gate, or late artifact; invalid outcome cardinality/completed result; code/predicate/reason/count/claim mismatch; invalid `T_ref`; missing evidence/policy; invalid endpoint/fixed-input/candidate-set prerequisite; premature execution; or other bypass, leave the affected gate `UNRESOLVED`, emit the exact invalid-authorization diagnostic, block progression, and set every workflow-authorization-dependent `MaterialResult` to `UNAVAILABLE`. Do not use `PROVISIONAL` for an absent or invalid required workflow authorization, never reinterpret or summarize rejection as ineligibility, and verify unrelated `MaterialResult`s retain their prior independently derived availability, freshness, canonical value, dependencies, and trace identity.
    - Treat a valid permitted ineligibility as a valid member of its gate’s closed union that authorizes only progression to realistic comparison; never represent, count, summarize, trace, accept, or publish it as gate completion, pass, or success, and preserve its exactly-false claims unchanged.
    - Only after both artifacts provide valid closed-union outcomes authorizing progression, run realistic comparison at the already-frozen common `T_ref`. For every realistic output, independently derive `AVAILABLE`, `PROVISIONAL`, or `UNAVAILABLE` from that output’s own required admissible inputs/evidence, dependencies, result-specific acceptance conditions, and applicable workflow authorizations; do not copy gate status into the result, require every gate to pass, or use any realistic-result status to upgrade, relabel, mutate, or retroactively validate an ineligibility artifact. Require complete corresponding baseline/replacement temperature vectors with identical node identities and units; the exact closed mutually exclusive loss categories with one owner per contribution; fixed-bucket reconciliation; and thermal feedback only by the design equation, with no temperature-induced term duplicated in fixed-temperature buckets.
    - Keep 0.60 mΩ classified as `USER_SPECIFIED_TARGET` throughout. Missing manufacturer or thermal evidence follows the local availability lattice and cannot fabricate evidence, complete a gate, create ineligibility outside the closed routes, or bypass ordering.
    - **Property 15: Ordered Device Comparison and Exclusive Attribution** - Closed Gates Before Common-Tref
    - _Bug_Condition: `comparisonGateArtifactCodeCardinalityPrerequisiteOrOrderInvalid(input)` or comparison attribution/reconciliation is invalid_
    - _Expected_Behavior: one pre-gate `T_ref`, exactly two distinct immutable pre-realistic artifacts with exact-one closed outcomes, permitted ineligibility as progression authorization without completion/pass/success, independent local availability for each realistic result, unresolved/rejected/malformed authorization forcing dependent results unavailable while unrelated results remain unchanged, and exclusive realistic attribution_
    - _Preservation: positive-current bridge conduction, 0.60 mΩ target classification, independent suitability constraints, gate ordering, and non-overlapping attribution remain unchanged_
    - _Requirements: 2.25, 2.26, 2.29–2.31, 2.35, 2.38, 2.41, 2.42, 3.3, 3.7, 3.11_

  - [x] 3.8 Implement synthetic/real qualification isolation and incomplete-48-V handling
    - Propagate `SYNTHETIC_TEST_ONLY` through every synthetic input/evidence/trace/output/export/headline; reject synthetic-real graph mixing and marker removal without rebuilding solely from real evidence. Synthetic qualification is always non-gating and cannot support product/manufacturer/safety/measured claims.
    - Gate real qualification only with the exact device and complete direct voltage/current/SOA/avalanche/package/isolation/thermal/declared limits, complete waveform or applicable conservative stress bound, DC voltage, derating, operating conditions, and separate passing results.
    - For incomplete/invalid/out-of-applicability 48 V evidence, list every gap and return non-gating `UNAVAILABLE` when no evaluable admissible value exists or `PROVISIONAL` only with an evaluable finite bound. It must not fail overall bugfix acceptance.
    - Missing hardware/manufacturer/analog/settling/root evidence follows the same local non-gating discipline and never becomes a real claim.
    - **Property 16: Qualification Evidence Isolation** - Synthetic Never Supports Real Claims
    - _Bug_Condition: incomplete or synthetic evidence gates real qualification_
    - _Expected_Behavior: only complete real exact-device evidence gates; incomplete 48 V remains informative and non-gating_
    - _Preservation: all declared suitability constraints remain independent of RDS(on) benefit_
    - _Requirements: 2.15, 2.16, 2.39, 2.41, 3.7_

  - [x] 3.9 Implement orthogonal firmware-implementation and hardware-measurement status
    - Publish independent axes, each exactly `VERIFIED`, `UNVERIFIED`, `PROVISIONAL`, or `UNAVAILABLE`, with separate complete predicates and global availability semantics.
    - Require immutable source/config/build/test evidence for firmware verification and independent exact-hardware/instrument/calibration/waveform/limit evidence for hardware verification. Missing fields prohibit `VERIFIED`; executed failed predicates produce `UNVERIFIED` with named failures.
    - Generate all status/evidence products and prove mutating one axis cannot alter or imply the other. Phase generation or PWM-stop cannot verify automatic no-load detection/control.
    - Missing hardware/manufacturer/analog/settling/root evidence remains local and non-gating. Do not implement or modify no-load detection, phase reduction, standby, sleep, or any firmware behavior.
    - **Property 17: Orthogonal Control Evidence** - Independent Status Axes
    - _Bug_Condition: firmware and hardware evidence are combined or inferred_
    - _Expected_Behavior: both axes remain independently traced without firmware changes_
    - _Preservation: unvalidated control outputs remain modelled/calculated rather than measured facts_
    - _Requirements: 2.27, 2.28, 2.40, 2.41, 3.9_

  - [x] 3.10 Implement publication, stale-result, and unsupported-claim gates
    - Make publication a projection of fresh accepted trace nodes. Require boundary, assumption, ATS, evidence/conflict, oracle, analog status, thermal case, both control axes, result basis, availability, freshness, trace identity/dependencies, uncertainty, and synthetic marker.
    - Reject stale, rounded-input, noncanonical, mixed-synthetic, fabricated-policy/evidence, unsupported loaded-constant, boundary-mismatched, and trace-incomplete publication. Recompute changed descendants with new identities before publication.
    - Publish every no-load category independently and preserve missing manufacturer/hardware/analog/settling/root evidence as visible local non-gating outcomes, never stronger claims or unrelated global failures.
    - **Property 20: Publication Safety** - Fresh Canonical Fully Traced Claims
    - _Bug_Condition: publication consumes stale/rounded/unsupported/mixed evidence or overstates unavailable facts_
    - _Expected_Behavior: only fresh trace-complete claims publish and affected gaps remain safely non-gating_
    - _Preservation: supported calculated/modelled results remain publishable under explicit boundaries/statuses_
    - _Requirements: 2.5, 2.32–2.35, 2.39, 2.41, 2.42, 3.1, 3.4, 3.5, 3.8, 3.9_

  - [x] 3.11 Run all unit, property-based, integration, trace, and publication tests
    - Run finite non-watch unit suites for canonical/MaterialResult semantics, evidence/transitions, and the exact 2.36 vectors: compiled `N=2285`, `H=1142`, actual `32000000/457 Hz` carrier, odd `1142/1143` intervals, `q=pH/100`, corrected p=10/1/0 comparators/raw/LV-or-complete-adjusted lists, p=10 complete adjusted count `16`, p=1/p=0 adjusted LV count `8`, tick-2284 OFF-before-ON ordering, and p=0 one-tick ownership. Also cover explicit unavailable suppressed-pulse/complete-adjusted-SR/complete-aggregate fields, analog deletion, ATS/threshold absence, residual matrix/settling, legacy manifest/adapters, domain isolation, thermal APIs, and the complete comparison-gate schema: `T_ref` frozen before either gate; exactly two distinct immutable non-aliased/non-reused pre-realistic artifacts and no extras; exactly one closed-union outcome per gate; exact `canonical_code` spelling/cardinality; every requirement-valid completed result; each controlled iff code and exact predicate set only after all other 2.29 preconditions pass; sole endpoint code only after all endpoint/evidence/policy/fixed-input/closed-candidate-set prerequisites pass and no eligible case exists; all-and-only predicates; specific reason; canonical zero evaluated count; exactly-once false completion/pass/success fields; valid permitted ineligibility authorizing progression without completion/pass/success; independent downstream realistic-result fixtures for `AVAILABLE`, `PROVISIONAL`, and `UNAVAILABLE` based on each result’s own admissible inputs/evidence, dependencies, result-specific acceptance conditions, and valid workflow authorization; proof that no downstream status upgrades/relabels/mutates the ineligibility artifact; and malformed/rejected/unresolved authorization forcing every dependent `MaterialResult` `UNAVAILABLE` while unrelated results remain unchanged. Also test qualification, controls, publication, and firmware manifests.
    - Run design Correctness Properties 3–20 with generators/shrinkers covering evidence histories, dependency DAGs, canonical display collisions, and one-field oracle mutations across `N`, `H`, odd interval lengths, `q`, `k`, actual carrier, comparators, available event identity/order/count, and availability tags. Generate stale `2286/1143`/even/exact-50% inputs, p=10 count `15`, tick-2284 ON-before-OFF, altered p=1/p=0 adjusted LV count/list, and every coercion of unavailable suppressed-pulse/adjusted-SR/aggregate fields to empty, zero, omitted, no-transition, no-switching, no-gate-loss, or loss-free forms; require rejection. Also cover absent policies, residual matrices, threshold boundaries, every excluded schema path, thermal exits/certificates, and every comparison-order permutation. Generate absent/late/nonfinite/unitless/out-of-domain/changed `T_ref`; every gate-artifact count other than exactly two; missing/duplicate/mutable/malformed/invalid/wrong-gate/aliased/reused/late/extra artifacts; shared identities; zero/duplicate/multiple/mixed outcomes; and invalid completed results. Cross every controlled current/conduction truth-table row with all three exact codes, generic/alias/wrong-gate codes, every predicate-set mutation, and failure of each other 2.29 precondition. Cross endpoint no-case coding with malformed/unevidenced/nonfinite/unordered/dimension-unit-applicability-mismatched endpoints, missing/invalid/unevidenced/inapplicable policy, unfixed inputs, open/mutable candidate sets, eligible and ineligible closed-set populations, and bypass attempts. Mutate `canonical_code`, all-and-only predicates, specific reason, canonical zero evaluated count, and exactly-once false `completion_claim`/`pass_claim`/`success_claim`; attempt to count, trace, accept, or publish ineligibility as completion/pass/success. Generate valid permitted-ineligibility authorizations crossed with realistic outputs whose local evidence makes them independently `AVAILABLE`, `PROVISIONAL`, or `UNAVAILABLE`, and reject every attempt to derive those statuses from gate pass/completion/success or to upgrade/relabel/mutate the ineligibility artifact. Generate malformed, rejected, unresolved, and operation-non-authorizing applicable gates; require every authorization-dependent result to be `UNAVAILABLE` while unrelated results preserve exact availability, freshness, canonical value, dependency set, and trace identity; reject every unresolved-state bypass or dependent `PROVISIONAL`/`AVAILABLE` result. Also cover overlapping buckets, qualification bundles, control products, no-load categories, stale publications, and firmware path/hash mutations.
    - Run integrations for evidence adjudication/staleness; exact p=10/1/0 digital vectors with `N=2285`, `H=1142`, `32000000/457 Hz`, 1142/1143 intervals, exact comparators/raw events, p=10 complete adjusted count `16`, p=1/p=0 adjusted LV count `8`, `SR_B_OFF` before `SR_B_ON` at tick `2284`, and p=0 one-tick `LEG_COMMAND_TICK_ASYMMETRY` ownership. Verify p=1/p=0 suppressed-pulse, complete adjusted SR, and complete aggregate list/count stay safety-relevant non-gating `UNAVAILABLE` and cannot become empty/zero/no transitions/no switching/no gate loss; also cover unavailable analog, settling absent/exceeded/pass paths, every residual matrix row, every legacy mapping, every excluded mutation, and all thermal modes. For comparison, freeze one valid `T_ref` before either gate, create exactly two distinct immutable non-reused pre-realistic artifacts with no extras, and exercise exactly one closed outcome per gate. Cover valid completed outcomes; all three controlled iff rows with exact `canonical_code` and all-and-only predicates after all other 2.29 preconditions pass; and endpoint `INELIGIBLE_NO_POSITIVE_CURRENT_CASE` only after valid evidenced finite ordered endpoints, valid applicable policy, fixed inputs, and a closed immutable candidate set all pass while no candidate is eligible. Require a specific reason, canonical evaluated count zero, and exactly one each of `completion_claim=false`, `pass_claim=false`, and `success_claim=false`. Reject wrong/generic/alias/predicate-mismatched codes, malformed/invalid/late/duplicate/reused/extra artifacts, invalid completed outcomes, missing evidence/policy, invalid endpoints, unfixed inputs, open/mutable candidate sets, bad counts/claims, and every bypass; verify every malformed, rejected, or unresolved gate blocks progression and forces all workflow-authorization-dependent `MaterialResult`s to `UNAVAILABLE`, while unrelated results retain exact prior status and trace. Verify a valid permitted ineligibility is a closed-union authorization for progression without being counted, traced, accepted, or published as completion/pass/success. After both artifacts authorize progression, run separate realistic common-`T_ref` fixtures whose local admissible evidence/dependencies/acceptance conditions produce `AVAILABLE`, `PROVISIONAL`, and `UNAVAILABLE`; verify these are independent classifications, never require the ineligibility gate to pass/complete/succeed, and never upgrade, relabel, mutate, or retroactively validate its artifact; reject thermal overlap. Complete synthetic/real qualification, orthogonal controls, and accepted/rejected publication integrations.
    - Tests must treat absent manufacturer/hardware/analog/settling/thermal/root evidence as the specified passing non-gating status outcome, not require fabricated “real” evidence. Confirm no test modifies firmware.
    - _Bug_Condition: corrected components fail under composition or depend on missing/fabricated external evidence_
    - _Expected_Behavior: all design properties pass with safe local unavailable/provisional/unproven outcomes_
    - _Preservation: all frozen observations and no-baseline routes remain executable_
    - _Requirements: 2.1–2.42, 3.1–3.12_

  - [x] 3.12 Re-run the original bug-condition exploration test
    - **Property 1: Expected Behavior** - Conservative Corrected Pipeline
    - Re-run the exact task-1 property, generators, deterministic fixtures, and counterexamples; do not replace them with post-fix-only tests.
    - Require every prior counterexample to satisfy `expectedBehavior(result)` through a fresh valid result or the exact local non-gating `PROVISIONAL`, `UNAVAILABLE`, or `UNPROVEN` outcome required for missing manufacturer/hardware/analog/settling/thermal/root evidence.
    - Confirm exact 2.36 compiled semantics and vectors: `N=2285`, `H=1142`, actual `32000000/457 Hz` carrier, odd `1142/1143` intervals, `q=pH/100`, corrected p=10/1/0 comparator/raw/available-adjusted values, p=10 complete adjusted count `16`, p=1/p=0 adjusted LV count `8`, `SR_B_OFF` before `SR_B_ON` at tick `2284`, and sole p=0 one-tick `LEG_COMMAND_TICK_ASYMMETRY` ownership. Require p=1/p=0 suppressed-pulse semantics, complete adjusted SR list/count, and complete aggregate adjusted list/count to remain safety-relevant non-gating `UNAVAILABLE` pending independent ESP-IDF peripheral-semantics evidence or measurement, and reject empty/zero/no-transition/no-switching/no-gate-loss coercion. Also confirm evidence/conflict integrity, no policy defaults, residual conservation, domain isolation, thermal semantics, qualification isolation, control independence, and publication freshness. For ordered comparison, confirm pre-gate frozen `T_ref`; exactly two distinct immutable non-reused pre-realistic gate artifacts and no extras; exact-one closed outcome per gate; exact `canonical_code`; controlled iff and endpoint sole-code prerequisites; all-and-only predicates, specific reason, canonical zero count, exactly-once false completion/pass/success fields; every malformed, rejected, or unresolved applicable gate blocking progression and making all workflow-authorization-dependent `MaterialResult`s `UNAVAILABLE` while unrelated results remain unchanged; each valid permitted ineligibility authorizing progression without completion/pass/success; and independently derived realistic `AVAILABLE`, `PROVISIONAL`, and `UNAVAILABLE` outputs that neither require every gate to pass nor upgrade, relabel, or mutate the ineligibility artifact.
    - **EXPECTED OUTCOME AFTER FIX**: The same Property 1 test that failed before implementation now passes.
    - _Requirements: 2.1–2.42, 3.11_

  - [x] 3.13 Re-run the frozen preservation suite
    - **Property 2: Preservation** - Frozen Legacy Routing
    - Re-run the exact task-2 manifest, routes, adapters, hashes, predicates, populations, sampling, observations, acceptance rules, and no-baseline fix checks; never recapture or update the baseline.
    - Require every `BASELINE_BEHAVIOR` comparison to pass and every `NO_BASELINE_BEHAVIOR` mapping to contain no observation and evaluate at least one fix-check case. Require exact closed key coverage and `generated ≥ eligible ≥ sampled = evaluated ≥ 1` with ordered IDs.
    - Reject adapter/applicability/hash/schema drift or omitted eligible cases. Investigate changed outputs through traces and accept only intended repairs outside the frozen observation.
    - Preserve safe non-gating `PROVISIONAL`, `UNAVAILABLE`, and `UNPROVEN` outcomes for absent manufacturer/hardware/analog/settling/thermal/root evidence where applicable.
    - **EXPECTED OUTCOME AFTER FIX**: The identical Property 2 suite still passes with complete non-vacuous coverage.
    - _Requirements: 2.13, 2.14, 2.41, 2.42, 3.1–3.12_

- [x] 4. Checkpoint - Capture final firmware hashes and enforce acceptance
  - **Property 18: Firmware Immutability** - Exact Before/After Manifest Equality
  - Capture the after manifest against the same named commit/tree and exact closed firmware paths frozen in task 1. Require exact canonical path-set/content-hash equality; any addition, removal, rename, config change, or hash change fails acceptance.
  - Require recorded pre-fix Property 1 failures and passing post-fix rerun; passing pre/post Property 2 suites with closed key coverage; and passing finite unit, property, integration, trace, and publication suites.
  - Require exact 2.36 p=10/1/0 validation with compiled `N=2285`, `H=1142`, actual `32000000/457 Hz` carrier, odd `1142/1143` intervals, `q=pH/100`, every corrected comparator/raw/available-adjusted vector, p=10 complete adjusted count `16`, p=1/p=0 adjusted LV count `8`, tick-2284 OFF-before-ON ordering, and exact p=0 one-tick ownership. Require p=1/p=0 suppressed-pulse semantics and complete adjusted SR/aggregate list/count to remain safety-relevant non-gating `UNAVAILABLE` pending independent ESP-IDF peripheral-semantics evidence or measurement, with tests rejecting empty/zero/no-transition/no-switching/no-gate-loss coercion. Also require canonical `MaterialResult`/ATS behavior; evidence/conflict lifecycle; unavailable analog handling; typed residual/settling behavior; domain isolation; thermal APIs/seeds/certificate semantics; and the finalized comparison contract: one finite unit-bearing in-domain `T_ref` frozen before either gate; exactly two distinct immutable non-aliased/non-reused pre-realistic gate-outcome artifacts, one per exact gate and no extras; exactly one gate-specific closed-union outcome each; exact `canonical_code`; controlled three-code iff routing only after every other 2.29 precondition passes; endpoint sole-code routing only after all endpoint/evidence/policy/fixed-input/closed-candidate-set prerequisites pass and no eligible case exists; all-and-only predicates, specific reason, canonical evaluated count zero, and exactly-once false completion/pass/success fields. Require tests to reject wrong/generic/alias/predicate-mismatched codes, malformed/invalid/late/duplicate/reused artifacts, invalid completed outcomes, missing evidence/policy, invalid endpoints, and every bypass; every malformed, rejected, or unresolved applicable gate must block progression and make every workflow-authorization-dependent `MaterialResult` `UNAVAILABLE`, while unrelated results remain unchanged. Require each valid permitted ineligibility to authorize progression without becoming completion/pass/success. After both artifacts authorize progression, require realistic common-`T_ref` outputs to be independently classified as `AVAILABLE`, `PROVISIONAL`, or `UNAVAILABLE` from their own admissible inputs/evidence, dependencies, result-specific acceptance conditions, and authorizations, without requiring all gates to pass and without upgrading, relabeling, or mutating an ineligibility artifact. Also require synthetic/real qualification separation, orthogonal controls, and stale-publication blocking.
  - Require unavailable dependencies to propagate `UNAVAILABLE`, evaluable bounded incomplete evidence to remain at most `PROVISIONAL`, and unsupported uniqueness to remain `UNPROVEN`. Unknowns must never become zero, manufacturer/direct/measured facts, or gating evidence.
  - Do not gate overall bugfix acceptance on absent manufacturer evidence, hardware measurements, unavailable analog fields, unavailable settling thresholds/observations, incomplete 48 V evidence, an optional missing root certificate, or missing independent ESP-IDF peripheral-semantics evidence for p=1/p=0 adjusted SR behavior. List each as a non-gating obligation with the affected decision and artifact gap; never summarize unavailable adjusted SR/aggregate behavior as empty, zero transitions, zero switching, or zero gate loss.
  - Publish final acceptance evidence with seeds/case IDs, preservation counts, artifact IDs/versions/hashes, traces, conflicts, non-gating statuses, qualification state, and exact before/after firmware equality. Confirm explicitly that no firmware source/configuration or behavior changed.
  - _Requirements: 2.1–2.42, 3.1–3.12_
